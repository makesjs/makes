exports.html = {
  echo: '<!--[ \t]*@echo[ \t]+(.*?)[ \t]*(?:-->|!>)',
  if: {
    start: '[ \t]*<!--[ \t]*@if[ \t]+(.*?)[ \t]*(?:-->|!>)(?:[ \t]*\n+)?',
    end  : '[ \t]*<!(?:--)?[ \t]*@endif[ \t]*(?:-->|!>)(?:[ \t]*\n)?'
  },
};

exports.js = {
  echo: [
    '/\\*[ \t]*@echo[ \t]+(.*?)[ \t]*\\*(?:\\*|/)',
    '//[ \t]*@echo[ \t]+(.*?)[ \t]*$'
  ],
  if: {
    start: '[ \t]*(?://|/\\*)[ \t]*@if[ \t]+([^\n*]*)(?:\\*(?:\\*|/))?(?:[ \t]*\n+)?',
    end  : '[ \t]*(?://|/\\*)[ \t]*@endif[ \t]*(?:\\*(?:\\*|/))?(?:[ \t]*\n)?'
  },
};
